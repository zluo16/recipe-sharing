<h1>All Recipes</h1>

<% @recipes.each do |recipe| %>
  <% if recipe.published %>
  <ul>
    <li><%= link_to recipe.name, recipe_path(recipe) %></li>
  </ul>
  <% else %>
    There are no published recipes
  <% end %>

<% end %>
